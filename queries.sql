
-- Search query

-- Get full product info
SELECT s_Name, p_name, p_price, p_release, p_lastShipment
FROM products

SELECT s_storeName
FROM Store

SELECT
FROM Contains

-- Insert query
INSERT INTO products(s_Name, p_name, p_price, p_releaseDate, p_lastShipment[DATE] ), Store(s_cityID, s_storeName,  s_storeNum, p_Name)
VALUES();

INSERT INTO locations(storeName, storeNum, c_ID, c_name)
VALUES();

INSERT INTO SupplyDemand(p_name, p_price, p_lastShipment[DATE], r_nintendoRating, r_storeRating)
VALUES();

INSERT INTO priceChngFreq(p_name, p_price, p_lastShipment[DATE], b_name, b_basePrice)
VALUES();


-- Delete query
